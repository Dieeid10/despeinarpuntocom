import { Facebook } from '@/icons/index'

export const Header = () => {
    return(        
        <header className="[grid-area:header;] bg-[var(--color-conteiner)] p-2">
            
        </header>
    )
}